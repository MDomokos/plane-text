// Stub. Owned by the size-test agent (spec 5.9, Test A). Owns sizeTestLog.
import { stubScreen } from '../screen.js';
import { register } from '../router.js';

export default register(stubScreen('settings/size-test', 'Size test'));
