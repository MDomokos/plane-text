// Stub. Owned by the charset-editor agent (spec 5.1). Owns customCharsets.
import { stubScreen } from '../screen.js';
import { register } from '../router.js';

export default register(stubScreen('settings/charsets', 'Charsets'));
