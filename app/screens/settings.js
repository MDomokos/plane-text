// Stub. Settings index. Links to size-test and charsets. Owns calibration.
import { stubScreen } from '../screen.js';
import { register } from '../router.js';

export default register(stubScreen('settings', 'Settings'));
